<div class="navbar navbar-inverse">
  <div class="navbar-inner container-fluid">
    <div class="col-sm-12">
      <div class="navbar-header">
        <div class="navbar-brand">
          <%= link_to (image_tag "logo.jpg", :alt => "Logo Here", :height => "41px"), root_path %>
        </div>
      </div>
      <ul class="nav navbar-nav">
        <li>
          <%= link_to "Home", root_path %>
        </li>
        <% if current_user.present? %>
         
          <% if current_user.teacher? %>
            <%= render "shared/teacher_side_bar" %>
 
              <% else %>
            <%= render "shared/student_side_bar" %>
                <% end %>

                 <li>
            <%= link_to "Your profile", registrations_show_path %>
          </li>

          <li>
            <%= link_to "Sign out", destroy_user_session_path, :data => {:method => :delete} %>
          </li>
         
        <% else %>
          <li>
            <%= link_to "Sign in", new_user_session_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
